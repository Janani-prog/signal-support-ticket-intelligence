# Signal — Support Ticket Intelligence Platform
# Python 3.11+ (developed/tested on 3.12). CPU-only throughout — no GPU dependency.

# --- Data / general ---
pandas==2.2.3
numpy==1.26.4
datasets==3.2.0
python-dotenv==1.0.1

# --- Classification (Phase 2) ---
scikit-learn==1.5.2
transformers==4.47.1
torch==2.5.1
accelerate==1.2.1

# --- Clustering (Phase 3) ---
sentence-transformers==3.3.1
umap-learn==0.5.7
hdbscan==0.8.40

# --- Retrieval + summarization (Phase 4) ---
chromadb==1.0.21

# --- Experiment tracking (Phase 0+) ---
mlflow==2.19.0

# --- API layer (Phase 5) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
slowapi==0.1.9

# --- Monitoring (Phase 8) ---
evidently==0.4.40

# --- Notebooks / EDA (Phase 1) ---
jupyter==1.1.1
matplotlib==3.10.0
seaborn==0.13.2

# --- Testing / hygiene ---
pytest==8.3.4
pip-audit==2.7.3
